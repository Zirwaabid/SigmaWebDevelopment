// find factorial of 6 using 2 methods

// 1-- by using reduce 
let arr = [1, 2, 3, 4, 5, 6];
const factorial = (a, b) => {
    return a * b
}
console.log(arr.reduce(factorial));

// 2--- by using for loop
